apiVersion: 1

datasources:
 - name: postgres-datasource
   type: postgres
   access: proxy
   url: db.:5434
   database: test
   user: postgres
   secureJsonData:
      password: 'postgres'
    jsondata:
      sslmode: 'disable'